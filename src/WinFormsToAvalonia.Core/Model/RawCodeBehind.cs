namespace WinFormsToAvalonia.Core.Model;

/// <summary>The original (non-designer) code-behind file for a Form, kept verbatim so it can be embedded as a preserved-for-migration comment block in the generated View.</summary>
public sealed record RawCodeBehind(string OriginalFilePath, string FullText);
